# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
.venv/
venv/
.python-version

# Environments
.env
.envrc

# IDE
.idea/
.vscode/
*.swp

# Claude Code — local per-machine settings/credentials (project settings.json IS tracked)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Tool caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Run outputs — don't check in generated JSON from translator runs
results/
submissions/
deletes/

# Cached upstream metadata pulled by the ingest tooling (regenerable, can be large)
data/mfd_metadata_cache/

# Human-readable run notes are committed (runs/<source>_<ACC>/), but keep the dir tracked when empty
